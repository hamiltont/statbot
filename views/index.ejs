<!DOCTYPE html>
<html lang="en">
<html>
  <head>
    <meta charset="utf-8">
    <title>Statbot - Overview page</title>
  </head>

    <body>
      <h1>Choose a user</h1>
      <ul>
        <% for (var i = 0 ; i < users.length; i++) { %>
	      <li><a href='<%= users[i] %>/'><%= users[i] %></a></li>
	<% } 
	   if (users.length == 0) { %>
              <li>Not connected to IRC yet, please refresh until connected</li>
	<% } %>
      </ul>
  </body>
</html>
